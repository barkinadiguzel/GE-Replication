# GE-Replication
GE-Replication is a faithful implementation of the Gather-Excite framework that improves CNN representations by injecting global context into local features. It provides flexible gather and excite operators to explore different ways of modeling feature context in convolutional networks.
